class Routes {
  static const main = '/';
  static const welcome = '/welcome';
  static const settings = '/settings';
  static const profileCreate = '/profile/create';
  static const vaccinationAdd = '/vaccination/add';
  static const vaccinationRecords = '/vaccination/records';

  /// Kept for backwards compatibility — maps to [main].
  static const dashboard = main;
}
